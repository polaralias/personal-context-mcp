# Server Configuration
PORT=3000
# BASE_URL: The public URL of the server. REQUIRED behind reverse proxies to ensure correct OAuth metadata and discovery headers.
BASE_URL=https://personal-context-mcp.polaralias.com

# Database
DATABASE_URL=sqlite:///data/mcp.db

# Security
MASTER_KEY=your-secure-master-key-here
REDIRECT_URI_ALLOWLIST=http://localhost:3010,https://mcp.yourdomain.com/connect

# Optional Security Overrides
# REDIRECT_URI_ALLOWLIST_MODE=exact
# CODE_TTL_SECONDS=300
# TOKEN_TTL_SECONDS=3600

# Authentication - Dual Auth Support
# MCP_API_KEY: A single API key for simple header/query auth
MCP_API_KEY=your-secret-api-key
# MCP_API_KEYS: Comma-separated list of additional valid API keys
# MCP_API_KEYS=key1,key2,key3

# Service Connectors (for local development)
GOOGLE_API_KEY=your-google-api-key
GOOGLE_POLL_CRON=0 0 * * *

# Docker Compose Environment Variables
# NOTE: The variables below are ONLY used when running via docker-compose.
# They use the PERSONAL_ prefix and are substituted into docker-compose.yml.
# The non-prefixed variables above (e.g., GOOGLE_API_KEY) are for local development.
# Uncomment and set values as needed when using docker-compose:
#
# PERSONAL_MASTER_KEY=your-secure-master-key-here
# PERSONAL_REDIRECT_URI_ALLOWLIST=https://chatgpt.com/,https://chat.openai.com/,http://localhost:3010/,http://localhost:3000/
# PERSONAL_REDIRECT_URI_ALLOWLIST_MODE=prefix
# PERSONAL_API_KEY_MODE=disabled
# PERSONAL_API_KEY_ISSUE_RATELIMIT=3
# PERSONAL_API_KEY_ISSUE_WINDOW_SECONDS=3600
# PERSONAL_MCP_RATELIMIT_PER_KEY=60
# PERSONAL_MCP_RATELIMIT_WINDOW_SECONDS=60
# PERSONAL_GOOGLE_API_KEY=your-google-api-key
# PERSONAL_GOOGLE_POLL_CRON=0 0 * * *
# PERSONAL_CODE_TTL_SECONDS=90
# PERSONAL_TOKEN_TTL_SECONDS=3600
